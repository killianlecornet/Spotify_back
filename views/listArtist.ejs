<!DOCTYPE html>
<html>
<head>
    <link rel="stylesheet" type="text/css" href="/css/list/style.css">
    <title>Liste des Artistes</title>
</head>
<body>
    <h1>Liste des Artistes</h1>

    <!-- Formulaire de recherche -->
    <form action="/artist" method="get">
        <input type="text" name="query" placeholder="Rechercher des artistes..." value="<%= query || '' %>">
        <button type="submit">Rechercher</button>
    </form>

    <!-- Affichage des artistes -->
    <ul>
        <% artists.forEach(artist => { %>
            <li>
                <div class="album-info">
                    <%= artist.name %>
                </div>
                <div class="btn-group">
                    <a class="btn btn-edit" href="/artist/edit/<%= artist._id %>">Éditer</a>
                    <form action="/artist/delete/<%= artist._id %>" method="post">
                        <button class="btn btn-delete" type="submit">Supprimer</button>
                    </form>
                </div>
            </li>
        <% }); %>
    </ul>
</body>
</html>
