<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <title>Éditer la Musique</title>
    <link rel="stylesheet" href="/css/edit/style.css"> <!-- Assurez-vous que le chemin est correct -->
</head>
<body>
    <div class="container">
        <h1>Éditer la Musique</h1>
        <form action="/music/update/<%= music._id %>" method="post" enctype="multipart/form-data">
            <div class="form-group">
                <label for="title">Titre de la musique:</label>
                <input type="text" id="title" name="title" value="<%= music.title %>" required>
            </div>

            <div class="form-group">
                <label for="artist">Artiste:</label>
                <select id="artist" name="artist" required>
                    <% artists.forEach(artist => { %>
                        <option value="<%= artist._id %>" <%= music.artist && music.artist._id && music.artist._id.equals(artist._id) ? 'selected' : '' %>><%= artist.name %></option>
                    <% }); %>
                </select>
            </div>

            <div class="form-group">
                <label for="genre">Genre:</label>
                <input type="text" id="genre" name="genre" value="<%= music.genre %>" required>
            </div>
            <!-- Autres champs... -->
            <button type="submit" class="btn-submit">Mettre à jour</button>
        </form>
    </div>
</body>
</html>
