<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <title>Ajouter de la musique</title>
</head>
<body>
    <h1>Ajouter une nouvelle musique</h1>
    <form action="/music/upload" method="post" enctype="multipart/form-data">
        <input type="text" name="title" placeholder="Titre de la musique" required>
        
        <!-- Modifiez le champ artiste pour être un menu déroulant (select) -->
        <select name="artist" required>
            <% artists.forEach(artist => { %>
                <option value="<%= artist._id %>"><%= artist.name %></option>
            <% }); %>
        </select>
        
        <input type="text" name="genre" placeholder="Genre" required>
        <input type="file" name="file" required> <!-- Fichier audio -->
        <input type="file" name="image" required> <!-- Image -->
        <button type="submit">Uploader</button>
    </form>
</body>
</html>
